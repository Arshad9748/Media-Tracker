import MediaPage from '../components/MediaPage'

const Animes = () => {
  return <MediaPage title="Anime" mediaType="anime" placeholder="Search for an anime (e.g. Monster)..." />
}

export default Animes